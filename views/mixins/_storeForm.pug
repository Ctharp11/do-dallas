mixin storeForm(store = {})
    form(action=`/add/${store.id || ''}` method="post" class="card" autocomplete="off" enctype="multipart/form-data")
        label(for="name") Name 
        input(type="text" name="name" value=store.name)
        label(for="description") Description
        textarea(name="description")= store.description
        //- photos
        label(for='photo') Add Photo
        input(type="file" name="photo" id="photo" accept="image/gif, image/png, image/jpeg" )
        if (store.photo)
            img(src=`/uploads/${store.photo}`, alt=`${store.name}`)
        //- location
        label(for="address") Address
        div.address-error
        input(type="text" id="address" name="location[address]" value=(store.location &&store.location.address))
        label(for="lat") Latitude
        input(type="text" id="lat" name="location[coordinates][1]" value=(store.location && store.location.coordinates[0]))
        label(for="lng") Longitude
        input(type="text" id="lng" name="location[coordinates][0]" value=(store.location && store.location.coordinates[1]))
        - const choices = ['Wifi', 'Open Late', 'Family Friendly', 'Vegetarian', 'Licensed', '$', '$$', '$$$'] 

        ul.tags
         - const tags = store.tags || []
            each choice in choices 
                .tag.tag__choice
                    input(type="checkbox" id=choice value=choice name="tags" checked=(tags.includes(choice)))
                    label(for=choice) #{choice}
        input(type="submit" value="Save" class="button")
