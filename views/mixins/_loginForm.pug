mixin loginForm() 
    form.form(action="/login" method="post")
        h2 Login
        label(for="email") Email
        input(type="email" name="email")
        label(for="password") Password
        input(type="password" name="password") 
        input.button(type="submit" class="get-loader" value="Log In")
